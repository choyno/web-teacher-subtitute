<tr>
	<td><%= room.name %></td>
	<td>
		<%= link_to edit_room_path(room) do %>
			<button class="btn btn-primary btn-blue">
				Edit
			</button>
		<% end %>
		<div class="delete">	
		<%= button_to "delete", room_path(room), :method=>:delete, :class=>"btn btn-primary btn-blue" %>
		</div>
	</td>	
</tr>