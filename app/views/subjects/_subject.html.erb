<tr>
	<td><%= subject.code %></td>
	<td><%= subject.name %></td>
	<td>
		<%= link_to edit_subject_path(subject) do %>
			<button class="btn btn-small btn-quaternary">
				Edit
			</button>
		<% end %>
		<div class="delete">
	<%= button_to "delete", subject_path(subject), :method=>:delete, :class=>"btn btn-primary btn-blue" %>
		</div>
	</td>	
</tr>